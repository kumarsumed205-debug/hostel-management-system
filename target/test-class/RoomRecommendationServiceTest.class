// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
package com.smarthostel;

import com.smarthostel.model.Room;
import com.smarthostel.service.RoomRecommendationService;
import java.util.List;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

class RoomRecommendationServiceTest {
   RoomRecommendationServiceTest() {
   }

   @Test
   void preferredRoomGetsHighestScore() {
      Room preferred = new Room(1, "A101", "A", 2, 0, "2-Seater", "AVAILABLE");
      Room other = new Room(2, "B201", "B", 3, 1, "3-Seater", "PARTIALLY_OCCUPIED");
      RoomRecommendationService service = new RoomRecommendationService();
      List<RoomRecommendationService.Recommendation> result = service.recommend(List.of(other, preferred), "A", "2-Seater");
      Assertions.assertEquals("A101", ((RoomRecommendationService.Recommendation)result.getFirst()).room().getRoomNumber());
      Assertions.assertEquals(100, ((RoomRecommendationService.Recommendation)result.getFirst()).score());
   }
}
